if (document.designMode == "on") {
  alert("Editing mode is now disabled! 😢");
  document.designMode = "off";
} else {
  alert("Editing mode is now enabled! 🎉🎉🎉");
  document.designMode = "on";
}
